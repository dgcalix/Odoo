<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="invoice.list">
      <field name="name">invoice list</field>
      <field name="model">invoice.invoice</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="invoice.action_window">
      <field name="name">invoice window</field>
      <field name="res_model">invoice.invoice</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="invoice.action_server">
      <field name="name">invoice server</field>
      <field name="model_id" ref="model_invoice_invoice"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="invoice" id="invoice.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="invoice.menu_1" parent="invoice.menu_root"/>
    <menuitem name="Menu 2" id="invoice.menu_2" parent="invoice.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="invoice.menu_1_list" parent="invoice.menu_1"
              action="invoice.action_window"/>
    <menuitem name="Server to list" id="invoice" parent="invoice.menu_2"
              action="invoice.action_server"/>
    -->
  </data>
</odoo>